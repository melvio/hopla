hopla complete bash - output the completion code for hopla

usage:
    hopla complete bash

Example
```bash
# Tip: to source the autocompletion, add the following to your `~/.bashrc`:
source <(hopla complete bash)
```
